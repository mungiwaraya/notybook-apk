# Disable directory browsing in js directory
Options -Indexes

# Prevent direct URL access to JavaScript files without local Referer
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteCond %{HTTP_REFERER} !^http(s)?://(www\.)?localhost [NC]
    RewriteCond %{HTTP_REFERER} !^http(s)?://(www\.)?127\.0\.0\.1 [NC]
    RewriteCond %{HTTP_REFERER} !^http(s)?://.*\.github\.io [NC]
    RewriteRule \.js$ /inote/index.html [R=302,L]
</IfModule>
